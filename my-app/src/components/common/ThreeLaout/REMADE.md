**属性**
```js
    leftWidth: PropTypes.number, //左栏宽度
    rightWidth: PropTypes.number, //右栏宽度
    minWidth: PropTypes.number, // 中间内容宽度
    gap: PropTypes.number,  // 三栏之间的间隙
    children: types.children, //中间内容
    left: PropTypes.node,  //左栏内容
    right: PropTypes.node //右栏内容
```